# Copy to secrets.env (gitignored) before running prime/launch.sh.
# WANDB_API_KEY is required for training metrics.
# HF_TOKEN is only needed if you switch to a gated model (Qwen3.5-9B is open).

WANDB_API_KEY=your-wandb-api-key-here
# HF_TOKEN=your-hf-token-here
